import javax.swing.*;
import java.awt.*;

public class Vue extends JFrame {

    JPanel panneauColor = new JPanel();

    Vue() {
        setTitle("Palette");
        setSize(800, 600);

        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}
